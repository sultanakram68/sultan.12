import { menuItemSchema } from "./menuItem";

/**
 * Sanity Schema Types Export
 * 
 * FOR STUDENTS:
 * When you have multiple schemas (e.g., MenuItem, Category, Testimonial), 
 * you group and export them together here so Sanity Studio knows about all of them.
 */
export const schemaTypes = [menuItemSchema];
